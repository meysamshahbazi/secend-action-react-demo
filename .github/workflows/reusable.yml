name: resusle deply
on: 
  # workflow_dispatch:
  push:
    branches:
      - master
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: output info
        run: echo "deply and uplading"

